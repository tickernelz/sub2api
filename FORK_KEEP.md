# Fork Keep Set

The canonical fork keep-set and upstream replay instructions now live in [`AGENTS.md`](AGENTS.md), especially section 2.

This file is retained as a compatibility pointer for existing links. Do not maintain a second copy of the feature list here; update `AGENTS.md` instead.
